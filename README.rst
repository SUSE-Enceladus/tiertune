tiertune
========

Configure cloud instances based on their specific instance type.

Please note that this project was only created to support systems
that do not have the `tuned` package available, or cannot use it for
some reason. If you have access to `tuned`, it is recommended to
use that instead, as it is a more robust and feature-rich solution.
